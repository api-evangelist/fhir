naftiko: 1.0.0-alpha2
info:
  label: Fast Healthcare Interoperability Resources (FHIR) — Questionnaire
  description: 'Fast Healthcare Interoperability Resources (FHIR) — Questionnaire. 9 operations. Lead operation: Fast Healthcare
    Interoperability Resources Search all resources of type Questionnaire based on a set of criteria. Self-contained Naftiko
    capability covering one Fhir business surface.'
  tags:
  - Fhir
  - Questionnaire
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FHIR_API_KEY: FHIR_API_KEY
capability:
  consumes:
  - type: http
    namespace: fhir-questionnaire
    baseUri: ''
    description: Fast Healthcare Interoperability Resources (FHIR) — Questionnaire business capability. Self-contained, no
      shared references.
    resources:
    - name: Questionnaire
      path: /Questionnaire
      operations:
      - name: searchquestionnaire
        method: GET
        description: Fast Healthcare Interoperability Resources Search all resources of type Questionnaire based on a set
          of criteria
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createquestionnaire
        method: POST
        description: Fast Healthcare Interoperability Resources Create a new resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: Questionnaire-_history
      path: /Questionnaire/_history
      operations:
      - name: histtypequestionnaire
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Questionnaire-rid
      path: /Questionnaire/{rid}
      operations:
      - name: readquestionnaire
        method: GET
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatequestionnaire
        method: PUT
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patchquestionnaire
        method: PATCH
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletequestionnaire
        method: DELETE
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: Questionnaire-rid-_history
      path: /Questionnaire/{rid}/_history
      operations:
      - name: histinstquestionnaire
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Questionnaire-rid-_history-hid
      path: /Questionnaire/{rid}/_history/{hid}
      operations:
      - name: vreadquestionnaire
        method: GET
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: fhir-questionnaire-rest
    port: 8080
    description: REST adapter for Fast Healthcare Interoperability Resources (FHIR) — Questionnaire. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/questionnaire
      name: questionnaire
      description: REST surface for Questionnaire.
      operations:
      - method: GET
        name: searchquestionnaire
        description: Fast Healthcare Interoperability Resources Search all resources of type Questionnaire based on a set
          of criteria
        call: fhir-questionnaire.searchquestionnaire
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createquestionnaire
        description: Fast Healthcare Interoperability Resources Create a new resource
        call: fhir-questionnaire.createquestionnaire
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/questionnaire/history
      name: questionnaire-history
      description: REST surface for Questionnaire-_history.
      operations:
      - method: GET
        name: histtypequestionnaire
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-questionnaire.histtypequestionnaire
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/questionnaire/{rid}
      name: questionnaire-rid
      description: REST surface for Questionnaire-rid.
      operations:
      - method: GET
        name: readquestionnaire
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        call: fhir-questionnaire.readquestionnaire
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatequestionnaire
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        call: fhir-questionnaire.updatequestionnaire
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchquestionnaire
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        call: fhir-questionnaire.patchquestionnaire
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletequestionnaire
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        call: fhir-questionnaire.deletequestionnaire
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/questionnaire/{rid}/history
      name: questionnaire-rid-history
      description: REST surface for Questionnaire-rid-_history.
      operations:
      - method: GET
        name: histinstquestionnaire
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-questionnaire.histinstquestionnaire
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/questionnaire/{rid}/history/{hid}
      name: questionnaire-rid-history-hid
      description: REST surface for Questionnaire-rid-_history-hid.
      operations:
      - method: GET
        name: vreadquestionnaire
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        call: fhir-questionnaire.vreadquestionnaire
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fhir-questionnaire-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fast Healthcare Interoperability Resources (FHIR) — Questionnaire. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: fast-healthcare-interoperability-resources-search
      description: Fast Healthcare Interoperability Resources Search all resources of type Questionnaire based on a set of
        criteria
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-questionnaire.searchquestionnaire
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-create
      description: Fast Healthcare Interoperability Resources Create a new resource
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: fhir-questionnaire.createquestionnaire
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-questionnaire.histtypequestionnaire
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-2
      description: Fast Healthcare Interoperability Resources Read the current state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-questionnaire.readquestionnaire
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-update
      description: Fast Healthcare Interoperability Resources Update the current state of the resource
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-questionnaire.updatequestionnaire
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-change
      description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
        - a series of change commands
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-questionnaire.patchquestionnaire
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-delete
      description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search etc)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: fhir-questionnaire.deletequestionnaire
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-3
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-questionnaire.histinstquestionnaire
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-4
      description: Fast Healthcare Interoperability Resources Read a past state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-questionnaire.vreadquestionnaire
      outputParameters:
      - type: object
        mapping: $.
