naftiko: 1.0.0-alpha2
info:
  label: Fast Healthcare Interoperability Resources (FHIR) — Location
  description: 'Fast Healthcare Interoperability Resources (FHIR) — Location. 9 operations. Lead operation: Fast Healthcare
    Interoperability Resources Search all resources of type Location based on a set of criteria. Self-contained Naftiko capability
    covering one Fhir business surface.'
  tags:
  - Fhir
  - Location
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FHIR_API_KEY: FHIR_API_KEY
capability:
  consumes:
  - type: http
    namespace: fhir-location
    baseUri: ''
    description: Fast Healthcare Interoperability Resources (FHIR) — Location business capability. Self-contained, no shared
      references.
    resources:
    - name: Location
      path: /Location
      operations:
      - name: searchlocation
        method: GET
        description: Fast Healthcare Interoperability Resources Search all resources of type Location based on a set of criteria
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: address
          in: query
          type: string
          description: '**A (part of the) address of the location**  '
        - name: address-city
          in: query
          type: string
          description: '**A city specified in an address**  '
        - name: address-postalcode
          in: query
          type: string
          description: '**A postal code specified in an address**  '
        - name: address-state
          in: query
          type: string
          description: '**A state specified in an address**  '
        - name: name
          in: query
          type: string
          description: '**A portion of the location''s name or alias**  '
      - name: createlocation
        method: POST
        description: Fast Healthcare Interoperability Resources Create a new resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: Location-_history
      path: /Location/_history
      operations:
      - name: histtypelocation
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Location-rid
      path: /Location/{rid}
      operations:
      - name: readlocation
        method: GET
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatelocation
        method: PUT
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patchlocation
        method: PATCH
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletelocation
        method: DELETE
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: Location-rid-_history
      path: /Location/{rid}/_history
      operations:
      - name: histinstlocation
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Location-rid-_history-hid
      path: /Location/{rid}/_history/{hid}
      operations:
      - name: vreadlocation
        method: GET
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: fhir-location-rest
    port: 8080
    description: REST adapter for Fast Healthcare Interoperability Resources (FHIR) — Location. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/location
      name: location
      description: REST surface for Location.
      operations:
      - method: GET
        name: searchlocation
        description: Fast Healthcare Interoperability Resources Search all resources of type Location based on a set of criteria
        call: fhir-location.searchlocation
        with:
          address: rest.address
          address-city: rest.address-city
          address-postalcode: rest.address-postalcode
          address-state: rest.address-state
          name: rest.name
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createlocation
        description: Fast Healthcare Interoperability Resources Create a new resource
        call: fhir-location.createlocation
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/location/history
      name: location-history
      description: REST surface for Location-_history.
      operations:
      - method: GET
        name: histtypelocation
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-location.histtypelocation
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/location/{rid}
      name: location-rid
      description: REST surface for Location-rid.
      operations:
      - method: GET
        name: readlocation
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        call: fhir-location.readlocation
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatelocation
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        call: fhir-location.updatelocation
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchlocation
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        call: fhir-location.patchlocation
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletelocation
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        call: fhir-location.deletelocation
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/location/{rid}/history
      name: location-rid-history
      description: REST surface for Location-rid-_history.
      operations:
      - method: GET
        name: histinstlocation
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-location.histinstlocation
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/location/{rid}/history/{hid}
      name: location-rid-history-hid
      description: REST surface for Location-rid-_history-hid.
      operations:
      - method: GET
        name: vreadlocation
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        call: fhir-location.vreadlocation
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fhir-location-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fast Healthcare Interoperability Resources (FHIR) — Location. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: fast-healthcare-interoperability-resources-search
      description: Fast Healthcare Interoperability Resources Search all resources of type Location based on a set of criteria
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-location.searchlocation
      with:
        address: tools.address
        address-city: tools.address-city
        address-postalcode: tools.address-postalcode
        address-state: tools.address-state
        name: tools.name
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-create
      description: Fast Healthcare Interoperability Resources Create a new resource
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: fhir-location.createlocation
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-location.histtypelocation
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-2
      description: Fast Healthcare Interoperability Resources Read the current state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-location.readlocation
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-update
      description: Fast Healthcare Interoperability Resources Update the current state of the resource
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-location.updatelocation
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-change
      description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
        - a series of change commands
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-location.patchlocation
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-delete
      description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search etc)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: fhir-location.deletelocation
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-3
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-location.histinstlocation
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-4
      description: Fast Healthcare Interoperability Resources Read a past state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-location.vreadlocation
      outputParameters:
      - type: object
        mapping: $.
