naftiko: 1.0.0-alpha2
info:
  label: Fast Healthcare Interoperability Resources (FHIR) — ServiceRequest
  description: 'Fast Healthcare Interoperability Resources (FHIR) — ServiceRequest. 9 operations. Lead operation: Fast Healthcare
    Interoperability Resources Search all resources of type ServiceRequest based on a set of criteria. Self-contained Naftiko
    capability covering one Fhir business surface.'
  tags:
  - Fhir
  - ServiceRequest
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FHIR_API_KEY: FHIR_API_KEY
capability:
  consumes:
  - type: http
    namespace: fhir-servicerequest
    baseUri: ''
    description: Fast Healthcare Interoperability Resources (FHIR) — ServiceRequest business capability. Self-contained, no
      shared references.
    resources:
    - name: ServiceRequest
      path: /ServiceRequest
      operations:
      - name: searchservicerequest
        method: GET
        description: Fast Healthcare Interoperability Resources Search all resources of type ServiceRequest based on a set
          of criteria
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _id
          in: query
          type: string
          description: '**Logical id of this artifact**  '
        - name: authored
          in: query
          type: string
          description: '**Date request signed**  '
        - name: category
          in: query
          type: string
          description: '**Classification of service**  '
        - name: code
          in: query
          type: string
          description: '**What is being requested/ordered**  '
        - name: patient
          in: query
          type: string
          description: '**Search by subject - a patient**  '
        - name: status
          in: query
          type: string
          description: '**draft | active | on-hold | revoked | completed | entered-in-error | unknown**  '
      - name: createservicerequest
        method: POST
        description: Fast Healthcare Interoperability Resources Create a new resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: ServiceRequest-_history
      path: /ServiceRequest/_history
      operations:
      - name: histtypeservicerequest
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: ServiceRequest-rid
      path: /ServiceRequest/{rid}
      operations:
      - name: readservicerequest
        method: GET
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateservicerequest
        method: PUT
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patchservicerequest
        method: PATCH
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteservicerequest
        method: DELETE
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: ServiceRequest-rid-_history
      path: /ServiceRequest/{rid}/_history
      operations:
      - name: histinstservicerequest
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: ServiceRequest-rid-_history-hid
      path: /ServiceRequest/{rid}/_history/{hid}
      operations:
      - name: vreadservicerequest
        method: GET
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: fhir-servicerequest-rest
    port: 8080
    description: REST adapter for Fast Healthcare Interoperability Resources (FHIR) — ServiceRequest. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/servicerequest
      name: servicerequest
      description: REST surface for ServiceRequest.
      operations:
      - method: GET
        name: searchservicerequest
        description: Fast Healthcare Interoperability Resources Search all resources of type ServiceRequest based on a set
          of criteria
        call: fhir-servicerequest.searchservicerequest
        with:
          _id: rest._id
          authored: rest.authored
          category: rest.category
          code: rest.code
          patient: rest.patient
          status: rest.status
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createservicerequest
        description: Fast Healthcare Interoperability Resources Create a new resource
        call: fhir-servicerequest.createservicerequest
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/servicerequest/history
      name: servicerequest-history
      description: REST surface for ServiceRequest-_history.
      operations:
      - method: GET
        name: histtypeservicerequest
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-servicerequest.histtypeservicerequest
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/servicerequest/{rid}
      name: servicerequest-rid
      description: REST surface for ServiceRequest-rid.
      operations:
      - method: GET
        name: readservicerequest
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        call: fhir-servicerequest.readservicerequest
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updateservicerequest
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        call: fhir-servicerequest.updateservicerequest
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchservicerequest
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        call: fhir-servicerequest.patchservicerequest
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteservicerequest
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        call: fhir-servicerequest.deleteservicerequest
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/servicerequest/{rid}/history
      name: servicerequest-rid-history
      description: REST surface for ServiceRequest-rid-_history.
      operations:
      - method: GET
        name: histinstservicerequest
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-servicerequest.histinstservicerequest
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/servicerequest/{rid}/history/{hid}
      name: servicerequest-rid-history-hid
      description: REST surface for ServiceRequest-rid-_history-hid.
      operations:
      - method: GET
        name: vreadservicerequest
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        call: fhir-servicerequest.vreadservicerequest
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fhir-servicerequest-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fast Healthcare Interoperability Resources (FHIR) — ServiceRequest. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: fast-healthcare-interoperability-resources-search
      description: Fast Healthcare Interoperability Resources Search all resources of type ServiceRequest based on a set of
        criteria
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-servicerequest.searchservicerequest
      with:
        _id: tools._id
        authored: tools.authored
        category: tools.category
        code: tools.code
        patient: tools.patient
        status: tools.status
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-create
      description: Fast Healthcare Interoperability Resources Create a new resource
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: fhir-servicerequest.createservicerequest
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-servicerequest.histtypeservicerequest
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-2
      description: Fast Healthcare Interoperability Resources Read the current state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-servicerequest.readservicerequest
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-update
      description: Fast Healthcare Interoperability Resources Update the current state of the resource
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-servicerequest.updateservicerequest
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-change
      description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
        - a series of change commands
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-servicerequest.patchservicerequest
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-delete
      description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search etc)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: fhir-servicerequest.deleteservicerequest
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-3
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-servicerequest.histinstservicerequest
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-4
      description: Fast Healthcare Interoperability Resources Read a past state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-servicerequest.vreadservicerequest
      outputParameters:
      - type: object
        mapping: $.
