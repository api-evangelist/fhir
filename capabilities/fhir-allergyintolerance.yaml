naftiko: 1.0.0-alpha2
info:
  label: Fast Healthcare Interoperability Resources (FHIR) — AllergyIntolerance
  description: 'Fast Healthcare Interoperability Resources (FHIR) — AllergyIntolerance. 9 operations. Lead operation: Fast
    Healthcare Interoperability Resources Search all resources of type AllergyIntolerance based on a set of criteria. Self-contained
    Naftiko capability covering one Fhir business surface.'
  tags:
  - Fhir
  - AllergyIntolerance
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FHIR_API_KEY: FHIR_API_KEY
capability:
  consumes:
  - type: http
    namespace: fhir-allergyintolerance
    baseUri: ''
    description: Fast Healthcare Interoperability Resources (FHIR) — AllergyIntolerance business capability. Self-contained,
      no shared references.
    resources:
    - name: AllergyIntolerance
      path: /AllergyIntolerance
      operations:
      - name: searchallergyintolerance
        method: GET
        description: Fast Healthcare Interoperability Resources Search all resources of type AllergyIntolerance based on a
          set of criteria
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: clinical-status
          in: query
          type: string
          description: '**active | inactive | resolved**  '
        - name: patient
          in: query
          type: string
          description: '**Who the sensitivity is for**  '
      - name: createallergyintolerance
        method: POST
        description: Fast Healthcare Interoperability Resources Create a new resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: AllergyIntolerance-_history
      path: /AllergyIntolerance/_history
      operations:
      - name: histtypeallergyintolerance
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: AllergyIntolerance-rid
      path: /AllergyIntolerance/{rid}
      operations:
      - name: readallergyintolerance
        method: GET
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateallergyintolerance
        method: PUT
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patchallergyintolerance
        method: PATCH
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteallergyintolerance
        method: DELETE
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: AllergyIntolerance-rid-_history
      path: /AllergyIntolerance/{rid}/_history
      operations:
      - name: histinstallergyintolerance
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: AllergyIntolerance-rid-_history-hid
      path: /AllergyIntolerance/{rid}/_history/{hid}
      operations:
      - name: vreadallergyintolerance
        method: GET
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: fhir-allergyintolerance-rest
    port: 8080
    description: REST adapter for Fast Healthcare Interoperability Resources (FHIR) — AllergyIntolerance. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/allergyintolerance
      name: allergyintolerance
      description: REST surface for AllergyIntolerance.
      operations:
      - method: GET
        name: searchallergyintolerance
        description: Fast Healthcare Interoperability Resources Search all resources of type AllergyIntolerance based on a
          set of criteria
        call: fhir-allergyintolerance.searchallergyintolerance
        with:
          clinical-status: rest.clinical-status
          patient: rest.patient
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createallergyintolerance
        description: Fast Healthcare Interoperability Resources Create a new resource
        call: fhir-allergyintolerance.createallergyintolerance
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/allergyintolerance/history
      name: allergyintolerance-history
      description: REST surface for AllergyIntolerance-_history.
      operations:
      - method: GET
        name: histtypeallergyintolerance
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-allergyintolerance.histtypeallergyintolerance
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/allergyintolerance/{rid}
      name: allergyintolerance-rid
      description: REST surface for AllergyIntolerance-rid.
      operations:
      - method: GET
        name: readallergyintolerance
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        call: fhir-allergyintolerance.readallergyintolerance
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updateallergyintolerance
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        call: fhir-allergyintolerance.updateallergyintolerance
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchallergyintolerance
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        call: fhir-allergyintolerance.patchallergyintolerance
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteallergyintolerance
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        call: fhir-allergyintolerance.deleteallergyintolerance
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/allergyintolerance/{rid}/history
      name: allergyintolerance-rid-history
      description: REST surface for AllergyIntolerance-rid-_history.
      operations:
      - method: GET
        name: histinstallergyintolerance
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-allergyintolerance.histinstallergyintolerance
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/allergyintolerance/{rid}/history/{hid}
      name: allergyintolerance-rid-history-hid
      description: REST surface for AllergyIntolerance-rid-_history-hid.
      operations:
      - method: GET
        name: vreadallergyintolerance
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        call: fhir-allergyintolerance.vreadallergyintolerance
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fhir-allergyintolerance-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fast Healthcare Interoperability Resources (FHIR) — AllergyIntolerance. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: fast-healthcare-interoperability-resources-search
      description: Fast Healthcare Interoperability Resources Search all resources of type AllergyIntolerance based on a set
        of criteria
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-allergyintolerance.searchallergyintolerance
      with:
        clinical-status: tools.clinical-status
        patient: tools.patient
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-create
      description: Fast Healthcare Interoperability Resources Create a new resource
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: fhir-allergyintolerance.createallergyintolerance
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-allergyintolerance.histtypeallergyintolerance
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-2
      description: Fast Healthcare Interoperability Resources Read the current state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-allergyintolerance.readallergyintolerance
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-update
      description: Fast Healthcare Interoperability Resources Update the current state of the resource
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-allergyintolerance.updateallergyintolerance
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-change
      description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
        - a series of change commands
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-allergyintolerance.patchallergyintolerance
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-delete
      description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search etc)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: fhir-allergyintolerance.deleteallergyintolerance
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-3
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-allergyintolerance.histinstallergyintolerance
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-4
      description: Fast Healthcare Interoperability Resources Read a past state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-allergyintolerance.vreadallergyintolerance
      outputParameters:
      - type: object
        mapping: $.
