naftiko: 1.0.0-alpha2
info:
  label: Fast Healthcare Interoperability Resources (FHIR) — Device
  description: 'Fast Healthcare Interoperability Resources (FHIR) — Device. 9 operations. Lead operation: Fast Healthcare
    Interoperability Resources Search all resources of type Device based on a set of criteria. Self-contained Naftiko capability
    covering one Fhir business surface.'
  tags:
  - Fhir
  - Device
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FHIR_API_KEY: FHIR_API_KEY
capability:
  consumes:
  - type: http
    namespace: fhir-device
    baseUri: ''
    description: Fast Healthcare Interoperability Resources (FHIR) — Device business capability. Self-contained, no shared
      references.
    resources:
    - name: Device
      path: /Device
      operations:
      - name: searchdevice
        method: GET
        description: Fast Healthcare Interoperability Resources Search all resources of type Device based on a set of criteria
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: patient
          in: query
          type: string
          description: '**Patient information, if the resource is affixed to a person**  '
        - name: status
          in: query
          type: string
          description: '**active | inactive | entered-in-error | unknown**  '
        - name: type
          in: query
          type: string
          description: '**The type of the device**  '
      - name: createdevice
        method: POST
        description: Fast Healthcare Interoperability Resources Create a new resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: Device-_history
      path: /Device/_history
      operations:
      - name: histtypedevice
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Device-rid
      path: /Device/{rid}
      operations:
      - name: readdevice
        method: GET
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatedevice
        method: PUT
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patchdevice
        method: PATCH
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletedevice
        method: DELETE
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: Device-rid-_history
      path: /Device/{rid}/_history
      operations:
      - name: histinstdevice
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Device-rid-_history-hid
      path: /Device/{rid}/_history/{hid}
      operations:
      - name: vreaddevice
        method: GET
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: fhir-device-rest
    port: 8080
    description: REST adapter for Fast Healthcare Interoperability Resources (FHIR) — Device. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/device
      name: device
      description: REST surface for Device.
      operations:
      - method: GET
        name: searchdevice
        description: Fast Healthcare Interoperability Resources Search all resources of type Device based on a set of criteria
        call: fhir-device.searchdevice
        with:
          patient: rest.patient
          status: rest.status
          type: rest.type
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createdevice
        description: Fast Healthcare Interoperability Resources Create a new resource
        call: fhir-device.createdevice
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/device/history
      name: device-history
      description: REST surface for Device-_history.
      operations:
      - method: GET
        name: histtypedevice
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-device.histtypedevice
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/device/{rid}
      name: device-rid
      description: REST surface for Device-rid.
      operations:
      - method: GET
        name: readdevice
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        call: fhir-device.readdevice
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatedevice
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        call: fhir-device.updatedevice
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchdevice
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        call: fhir-device.patchdevice
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletedevice
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        call: fhir-device.deletedevice
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/device/{rid}/history
      name: device-rid-history
      description: REST surface for Device-rid-_history.
      operations:
      - method: GET
        name: histinstdevice
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-device.histinstdevice
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/device/{rid}/history/{hid}
      name: device-rid-history-hid
      description: REST surface for Device-rid-_history-hid.
      operations:
      - method: GET
        name: vreaddevice
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        call: fhir-device.vreaddevice
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fhir-device-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fast Healthcare Interoperability Resources (FHIR) — Device. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: fast-healthcare-interoperability-resources-search
      description: Fast Healthcare Interoperability Resources Search all resources of type Device based on a set of criteria
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-device.searchdevice
      with:
        patient: tools.patient
        status: tools.status
        type: tools.type
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-create
      description: Fast Healthcare Interoperability Resources Create a new resource
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: fhir-device.createdevice
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-device.histtypedevice
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-2
      description: Fast Healthcare Interoperability Resources Read the current state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-device.readdevice
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-update
      description: Fast Healthcare Interoperability Resources Update the current state of the resource
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-device.updatedevice
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-change
      description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
        - a series of change commands
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-device.patchdevice
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-delete
      description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search etc)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: fhir-device.deletedevice
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-3
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-device.histinstdevice
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-4
      description: Fast Healthcare Interoperability Resources Read a past state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-device.vreaddevice
      outputParameters:
      - type: object
        mapping: $.
