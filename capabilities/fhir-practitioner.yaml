naftiko: 1.0.0-alpha2
info:
  label: Fast Healthcare Interoperability Resources (FHIR) — Practitioner
  description: 'Fast Healthcare Interoperability Resources (FHIR) — Practitioner. 9 operations. Lead operation: Fast Healthcare
    Interoperability Resources Search all resources of type Practitioner based on a set of criteria. Self-contained Naftiko
    capability covering one Fhir business surface.'
  tags:
  - Fhir
  - Practitioner
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FHIR_API_KEY: FHIR_API_KEY
capability:
  consumes:
  - type: http
    namespace: fhir-practitioner
    baseUri: ''
    description: Fast Healthcare Interoperability Resources (FHIR) — Practitioner business capability. Self-contained, no
      shared references.
    resources:
    - name: Practitioner
      path: /Practitioner
      operations:
      - name: searchpractitioner
        method: GET
        description: Fast Healthcare Interoperability Resources Search all resources of type Practitioner based on a set of
          criteria
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _id
          in: query
          type: string
          description: '**Logical id of this artifact**  '
        - name: identifier
          in: query
          type: string
          description: '**A practitioner''s Identifier**  '
        - name: name
          in: query
          type: string
          description: '**A server defined search that may match any of the string fields in the HumanName, including family,
            give, prefix, suffix, suffix, and/or text**  '
      - name: createpractitioner
        method: POST
        description: Fast Healthcare Interoperability Resources Create a new resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: Practitioner-_history
      path: /Practitioner/_history
      operations:
      - name: histtypepractitioner
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Practitioner-rid
      path: /Practitioner/{rid}
      operations:
      - name: readpractitioner
        method: GET
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatepractitioner
        method: PUT
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patchpractitioner
        method: PATCH
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletepractitioner
        method: DELETE
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: Practitioner-rid-_history
      path: /Practitioner/{rid}/_history
      operations:
      - name: histinstpractitioner
        method: GET
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: _since
          in: query
          type: string
          description: Only include resource versions that were created at or after the given instant in time
        - name: _at
          in: query
          type: string
          description: Only include resource versions that were current at some point during the time period specified in
            the date time value (see Search notes on date searching)
        - name: _list
          in: query
          type: string
          description: Only include resource versions that are referenced in the specified list (current list references are
            allowed)
    - name: Practitioner-rid-_history-hid
      path: /Practitioner/{rid}/_history/{hid}
      operations:
      - name: vreadpractitioner
        method: GET
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: fhir-practitioner-rest
    port: 8080
    description: REST adapter for Fast Healthcare Interoperability Resources (FHIR) — Practitioner. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/practitioner
      name: practitioner
      description: REST surface for Practitioner.
      operations:
      - method: GET
        name: searchpractitioner
        description: Fast Healthcare Interoperability Resources Search all resources of type Practitioner based on a set of
          criteria
        call: fhir-practitioner.searchpractitioner
        with:
          _id: rest._id
          identifier: rest.identifier
          name: rest.name
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createpractitioner
        description: Fast Healthcare Interoperability Resources Create a new resource
        call: fhir-practitioner.createpractitioner
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/practitioner/history
      name: practitioner-history
      description: REST surface for Practitioner-_history.
      operations:
      - method: GET
        name: histtypepractitioner
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-practitioner.histtypepractitioner
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/practitioner/{rid}
      name: practitioner-rid
      description: REST surface for Practitioner-rid.
      operations:
      - method: GET
        name: readpractitioner
        description: Fast Healthcare Interoperability Resources Read the current state of the resource
        call: fhir-practitioner.readpractitioner
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatepractitioner
        description: Fast Healthcare Interoperability Resources Update the current state of the resource
        call: fhir-practitioner.updatepractitioner
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchpractitioner
        description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
          - a series of change commands
        call: fhir-practitioner.patchpractitioner
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletepractitioner
        description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search
          etc)
        call: fhir-practitioner.deletepractitioner
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/practitioner/{rid}/history
      name: practitioner-rid-history
      description: REST surface for Practitioner-rid-_history.
      operations:
      - method: GET
        name: histinstpractitioner
        description: Fast Healthcare Interoperability Resources Read the past states of the resource
        call: fhir-practitioner.histinstpractitioner
        with:
          _since: rest._since
          _at: rest._at
          _list: rest._list
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/practitioner/{rid}/history/{hid}
      name: practitioner-rid-history-hid
      description: REST surface for Practitioner-rid-_history-hid.
      operations:
      - method: GET
        name: vreadpractitioner
        description: Fast Healthcare Interoperability Resources Read a past state of the resource
        call: fhir-practitioner.vreadpractitioner
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fhir-practitioner-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fast Healthcare Interoperability Resources (FHIR) — Practitioner. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: fast-healthcare-interoperability-resources-search
      description: Fast Healthcare Interoperability Resources Search all resources of type Practitioner based on a set of
        criteria
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-practitioner.searchpractitioner
      with:
        _id: tools._id
        identifier: tools.identifier
        name: tools.name
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-create
      description: Fast Healthcare Interoperability Resources Create a new resource
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: fhir-practitioner.createpractitioner
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-practitioner.histtypepractitioner
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-2
      description: Fast Healthcare Interoperability Resources Read the current state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-practitioner.readpractitioner
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-update
      description: Fast Healthcare Interoperability Resources Update the current state of the resource
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-practitioner.updatepractitioner
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-change
      description: Fast Healthcare Interoperability Resources Change the current state of the resource by providing a patch
        - a series of change commands
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: fhir-practitioner.patchpractitioner
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-delete
      description: Fast Healthcare Interoperability Resources Delete the resource so that it no exists (no read, search etc)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: fhir-practitioner.deletepractitioner
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-3
      description: Fast Healthcare Interoperability Resources Read the past states of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-practitioner.histinstpractitioner
      with:
        _since: tools._since
        _at: tools._at
        _list: tools._list
      outputParameters:
      - type: object
        mapping: $.
    - name: fast-healthcare-interoperability-resources-read-4
      description: Fast Healthcare Interoperability Resources Read a past state of the resource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fhir-practitioner.vreadpractitioner
      outputParameters:
      - type: object
        mapping: $.
